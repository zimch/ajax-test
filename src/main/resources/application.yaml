spring:
  h2:
    console:
      enabled: true
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:phonebook
    username: root
    password:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update